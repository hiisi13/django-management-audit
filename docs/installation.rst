============
Installation
============

At the command line::

    $ easy_install django-management-audit

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-management-audit
    $ pip install django-management-audit